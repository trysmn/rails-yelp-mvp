<div class="banner" style="background-image: linear-gradient(-225deg, rgba(0,101,168,0.6) 0%, rgba(0,36,61,0.6) 50%), url('<%= @restaurant.img_url %>');">
  <div class="banner-content">
    <h1><%= @restaurant.name %></h1>
    <a class="btn btn-primary btn-lg">Find out more</a>
  </div>
</div>

<%= render 'reviews' %>

<%= render 'reviews/form' %>
